import { StyleSheet } from "react-native";

export const AddButtonStyles = StyleSheet.create({
    addButton: {
        backgroundColor: "#4b8569",
        borderWidth: 0,
        paddingVertical: 12,
        paddingHorizontal: 20,
        fontSize: 20,
        flexDirection: 'row',
        justifyContent: 'center',
    }
})